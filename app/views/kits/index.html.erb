
<h3>Welcome <%= current_user.name %></h3>

<h4>List of Kits</h3>

<div>
  <% @kits.each do |k| %>
  <%= link_to k.name, kit_path(k) %><br><br>
  <% end %>
</div>

<%= link_to 'Add a New Kit', new_kit_path(current_user) %><br>
<%= link_to 'Sign Out', signout_path, method: :post %>
