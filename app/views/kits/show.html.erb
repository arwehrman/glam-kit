<h1><%= @kit.name %></h1>

<table style="width:75%">
  <tr>
    <th>Name</th>
    <th>Brand</th>
    <th>Color</th>
    <th>Price</th>
    <th>Rating</th>
  </tr>

  <tr>
    <% @kit.items.each do |i| %>
  </tr>
  <tr>
    <td><%= i.name %></td>
    <td><%= i.brand %></td>
    <td><%= i.color %></td>
    <td><%= i.price.to_f %></td> <!-- don't like the format need to change -->
    <td><%= i.rating %></td>
      <td><%= link_to "Edit", edit_kit_item_path(i) %></td>
      <td>Delete</td><!--this should be a link to edit item -->
  </tr>
  <% end %>
</table>
<p>Link to add another item</p><!--need link to add another item -->
<%= link_to "Delete Kit", kit_path(@kit), method: :delete, data: { confirm: "Are you sure?" }  %>
<%= link_to "Go Back to Kits", kits_path %><br><br>
